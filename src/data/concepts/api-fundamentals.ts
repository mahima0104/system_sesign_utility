import type { ConceptDeepDive } from '../../types';

export const apiDeepDives: ConceptDeepDive[] = [
  {
    moduleId: 'what-is-an-api',
    tagline: 'An API is a contract that lets two software systems talk without knowing each other\'s internals.',
    introduction: {
      layman: 'An API is a controlled doorway into a system. Your phone app does not open the bank database directly; it asks the bank API for account balance, transfer status, or card details. The API decides what is allowed, talks to internal systems, and returns a clear response.',
      analogy: 'Like ordering food at a restaurant: you do not walk into the kitchen and touch the stove. You read the menu, place an order with the waiter, and the waiter brings back the result.',
      whyMatters: 'Almost every real system design has APIs: mobile apps, web frontends, partner integrations, internal microservices, payment providers, identity providers, analytics pipelines, and admin dashboards. Interviewers expect you to explain API contracts, request/response flow, errors, security, versioning, and operational limits.',
    },
    subTopics: [
      {
        title: 'API as a Contract',
        icon: '📜',
        layman: 'An API says: if you send this kind of request, I promise to respond in this kind of way.',
        technical: 'A good API contract defines endpoint or operation name, method, parameters, request body, response body, status codes, authentication requirements, rate limits, error format, compatibility guarantees, and versioning policy. The implementation behind the API can change as long as the contract stays stable.',
        example: 'POST /v1/transfers with amount, currency, sourceAccountId, destinationAccountId, and idempotencyKey returns a transfer id, status, timestamp, and failure reason if rejected.',
        whenToUse: 'Use API contracts whenever one team, service, app, or external partner depends on another system.',
      },
      {
        title: 'How APIs Power Applications',
        icon: '📱',
        layman: 'The app screen you see is usually assembled by calling many APIs behind the scenes.',
        technical: 'A banking dashboard may call account APIs, transaction APIs, card APIs, loan APIs, notification APIs, and fraud APIs. The frontend sends requests over HTTPS, the API validates identity and input, calls services/databases, then returns JSON or another response format.',
        example: 'PayBank mobile home screen calls GET /accounts, GET /transactions?limit=10, GET /cards, and GET /offers. The user sees one screen, but many API calls built it.',
      },
      {
        title: 'Types of APIs',
        icon: '🧭',
        layman: 'Some APIs are public, some are only for partners, and some are private inside the company.',
        technical: 'Public APIs are exposed to outside developers with documentation and keys. Partner APIs are restricted to approved businesses. Internal APIs are used between services. Composite/BFF APIs aggregate multiple backend calls for a specific frontend. Each type has different stability, security, and documentation requirements.',
        example: 'Stripe exposes public payment APIs, Shopify exposes partner/admin APIs, and a bank may keep ledger APIs private inside the organization.',
      },
      {
        title: 'Using an API Step by Step',
        icon: '🧪',
        layman: 'To use an API, you read the docs, authenticate, send a request, handle the response, and handle errors.',
        technical: 'A production client should include timeouts, retries for safe operations, idempotency for writes, validation of response schema, structured logging, correlation ids, and handling for 4xx/5xx/rate-limit errors.',
        example: 'A fintech partner creates a payout by sending an OAuth token, an idempotency key, JSON payload, and a trace id. If it receives 202 Accepted, it polls or waits for a webhook.',
      },
    ],
    comparison: {
      caption: 'Common API visibility levels',
      columns: ['Type', 'Audience', 'Example', 'Main concern'],
      rows: [
        ['Public', 'Any developer', 'GitHub REST API', 'Documentation, rate limits, abuse protection'],
        ['Partner', 'Approved companies', 'Bank payout API', 'Contracts, security, compliance'],
        ['Internal', 'Company services', 'Ledger service API', 'Reliability, compatibility, observability'],
        ['BFF', 'One frontend', 'Mobile home API', 'Latency and UI-specific aggregation'],
      ],
    },
    realWorldExamples: [
      { company: 'PayBank', icon: '🏦', description: 'Mobile apps call APIs for login, balances, transfers, statements, and card controls instead of connecting to core banking systems directly.' },
      { company: 'Stripe', icon: '💳', description: 'Payment APIs hide complex card networks behind simple contracts like payment intents, customers, refunds, and webhooks.' },
      { company: 'Google Maps', icon: '🗺️', description: 'Apps use location APIs to embed maps, route calculations, geocoding, and places search without owning map infrastructure.' },
      { company: 'Corporate HR portal', icon: '🏢', description: 'Internal APIs expose employee profile, payroll, leave balance, and approvals to web and mobile clients.' },
    ],
    interviewQuestions: [
      {
        question: 'How would you explain an API to a beginner and then to a senior engineer?',
        answer: 'For a beginner: an API is a menu for software; you ask for something in a defined way and get a defined response. For a senior engineer: an API is a compatibility contract with behavior, security, errors, versioning, latency, limits, and ownership. The hard part is not the endpoint; it is keeping the contract reliable as systems and clients evolve.',
      },
      {
        question: 'What should you include when designing an API contract?',
        answer: 'Resource or operation names, methods, request fields, response schema, status codes, error format, auth model, permissions, rate limits, pagination, idempotency requirements, versioning, backward compatibility rules, examples, and observability fields such as request ids.',
      },
      {
        question: 'Why should a frontend not directly access a database?',
        answer: 'It would expose credentials, bypass authorization rules, couple UI to schema, make migrations dangerous, and remove a place to validate, rate-limit, log, and enforce business rules. APIs provide a controlled boundary.',
      },
    ],
    commonMistakes: [
      'Designing an API as only a URL and JSON shape, without defining errors, auth, limits, idempotency, and versioning.',
      'Letting frontend screens depend directly on internal database schema.',
      'Returning inconsistent error formats from different endpoints.',
      'Changing response fields without a migration plan for older clients.',
      'Not documenting examples for success, validation failure, authorization failure, and rate limiting.',
    ],
  },
  {
    moduleId: 'idempotency',
    tagline: 'Idempotency makes retries safe by ensuring the same operation has the same effect even if it is attempted more than once.',
    introduction: {
      layman: 'If your phone loses network after you tap Pay, the app may retry. Idempotency makes sure the bank does not debit you twice.',
      analogy: 'Like pressing an elevator button multiple times. The elevator should still come once; pressing again should not schedule five elevators.',
      whyMatters: 'Distributed systems retry because networks fail, servers time out, and clients disconnect. Without idempotency, retries can create duplicate orders, double payments, repeated emails, duplicated tickets, or corrupted counters.',
    },
    subTopics: [
      {
        title: 'Safe Retries',
        icon: '🔁',
        layman: 'A retry should not accidentally repeat the business action.',
        technical: 'GET, PUT, and DELETE are generally expected to be idempotent by HTTP semantics, while POST usually is not unless the API adds idempotency keys. For money-moving POST requests, clients send a unique key and the server stores the first result under that key.',
        example: 'POST /transfers with Idempotency-Key: abc123 creates transfer T1. If the same request retries with abc123, the server returns T1 again instead of creating T2.',
        whenToUse: 'Use idempotency for payments, transfers, order creation, booking, subscription changes, webhook consumers, and queue consumers.',
      },
      {
        title: 'Idempotency Key Store',
        icon: '🗄️',
        layman: 'The server remembers that it already handled this exact request.',
        technical: 'Store idempotency key, request fingerprint, response body/status, operation id, creation time, and expiry. The fingerprint prevents a client from reusing the same key with a different amount or destination. TTL prevents unbounded storage growth.',
        example: 'A bank stores key abc123 with hash(amount=500, to=merchant42), result=success, transferId=T1, expiresAt=24h.',
      },
      {
        title: 'Concurrency and Race Conditions',
        icon: '🏁',
        layman: 'Two retries can arrive at the same time, so checking then writing is not enough.',
        technical: 'Use a unique constraint or atomic insert on the idempotency key. The first request wins and marks the operation as in-progress or completed. Concurrent duplicates wait, poll, or return a 409/425-style response depending on API policy.',
        example: 'Two mobile retries hit different API servers. Both insert key abc123; only one succeeds because the database has UNIQUE(idempotency_key).',
      },
      {
        title: 'Limits and Trade-Offs',
        icon: '⚠️',
        layman: 'Idempotency protects against duplicates, but it does not magically make every operation safe.',
        technical: 'Idempotency requires storage, TTL, careful request hashing, response replay, and clear behavior for in-progress operations. It cannot fix non-deterministic side effects unless those effects are also guarded.',
        example: 'Sending an email inside the transfer transaction can still duplicate if email sending is outside the idempotent boundary. Use outbox/events for side effects.',
      },
    ],
    comparison: {
      caption: 'HTTP methods and idempotency expectations',
      columns: ['Method', 'Typical behavior', 'Example', 'Caution'],
      rows: [
        ['GET', 'Idempotent read', 'GET /accounts/123', 'Should not mutate state'],
        ['PUT', 'Idempotent replace/update', 'PUT /profile/address', 'Same body should produce same state'],
        ['DELETE', 'Idempotent removal', 'DELETE /cards/7', 'Second delete can return 404 or 204 by policy'],
        ['POST', 'Not idempotent by default', 'POST /transfers', 'Needs idempotency key for safe retry'],
      ],
    },
    realWorldExamples: [
      { company: 'Payment APIs', icon: '💳', description: 'Payment providers require idempotency keys so client retries do not charge the customer twice.' },
      { company: 'E-commerce checkout', icon: '🛒', description: 'Order creation uses client-generated request ids to avoid duplicate orders after timeouts.' },
      { company: 'Webhook consumers', icon: '🪝', description: 'Receivers store event ids so provider retries do not process the same event twice.' },
      { company: 'Message queues', icon: '📬', description: 'Consumers dedupe message ids because at-least-once delivery can redeliver the same message.' },
    ],
    interviewQuestions: [
      {
        question: 'How would you make a payment API idempotent?',
        answer: 'Require an Idempotency-Key header for create-payment requests, validate a request fingerprint, atomically insert the key with an in-progress marker, perform the payment once, store the final response, and replay that response for duplicates. Use TTL and monitor conflicts.',
      },
      {
        question: 'Why is client-side button disabling not enough?',
        answer: 'The client can crash, retry after timeout, send duplicate requests from multiple devices, or be malicious. The server is the authoritative place to dedupe side effects.',
      },
      {
        question: 'What do you do if the same idempotency key is reused with a different payload?',
        answer: 'Reject it, usually with 400 or 409, because accepting it would hide a client bug and could return a response for a different business operation.',
      },
    ],
    commonMistakes: [
      'Thinking idempotency means “do not retry.” It actually makes retry safe.',
      'Storing only the key but not the request fingerprint.',
      'Using an in-memory key store in a horizontally scaled API fleet.',
      'Creating the side effect before atomically reserving the idempotency key.',
      'Forgetting idempotency in webhook consumers and queue consumers.',
    ],
  },
  {
    moduleId: 'data-formats',
    tagline: 'Data formats decide how systems encode information when they talk to each other.',
    introduction: {
      layman: 'When one system sends data to another, both sides must agree on the language: JSON, XML, Protobuf, Avro, MessagePack, or something else.',
      analogy: 'Like sending documents: plain text is easy for anyone to read, a PDF preserves layout, and a compressed binary file is smaller but needs the right software to open.',
      whyMatters: 'Data format affects developer experience, payload size, latency, CPU usage, schema evolution, compatibility, debugging, and ecosystem support. Public APIs often optimize for readability; internal high-throughput APIs often optimize for compactness and contracts.',
    },
    subTopics: [
      {
        title: 'JSON',
        icon: '🧾',
        layman: 'JSON is readable, flexible, and the default for most web APIs.',
        technical: 'JSON is text-based, language-neutral, easy to inspect, and works naturally with browsers. It is verbose, lacks strict built-in schema, and has ambiguity around numbers, dates, enums, and binary data unless conventions are defined.',
        example: 'GET /accounts/123 returns {"id":"123","balance":{"amount":"2500.00","currency":"INR"}}.',
        whenToUse: 'Use JSON for public REST APIs, mobile APIs, admin APIs, and partner integrations where debuggability matters.',
      },
      {
        title: 'XML',
        icon: '📄',
        layman: 'XML is older and more verbose, but still common in enterprise and banking systems.',
        technical: 'XML supports schemas, namespaces, attributes, and document validation. SOAP APIs often use XML. It is heavy and harder to work with than JSON for many modern web clients, but mature in legacy enterprise stacks.',
        example: 'A bank partner may still send ISO-like XML messages for settlement or reconciliation.',
      },
      {
        title: 'Binary Formats',
        icon: '📦',
        layman: 'Binary formats are harder to read by humans but faster and smaller for machines.',
        technical: 'Protobuf, Avro, Thrift, and MessagePack reduce payload size and parse overhead. Protobuf and Thrift are schema-first and generate typed clients. Avro is common in data pipelines because schema travels well with events.',
        example: 'Internal risk scoring uses Protobuf over gRPC because every checkout calls it and latency matters.',
        whenToUse: 'Use binary formats for high-throughput internal services, streaming events, telemetry, or data pipelines where strict schemas and efficiency matter.',
      },
      {
        title: 'Schema Evolution',
        icon: '🔄',
        layman: 'APIs change over time. Old clients should not break when new fields appear.',
        technical: 'Compatible changes include adding optional fields, adding enum values carefully, and preserving field meanings. Breaking changes include renaming required fields, changing types, removing fields, or changing semantics. Schema registries help event systems enforce compatibility.',
        example: 'Add transfer.referenceNote as optional. Do not rename amount to value without a new version or migration plan.',
      },
    ],
    comparison: {
      caption: 'Choosing a format',
      columns: ['Format', 'Human readable', 'Performance', 'Best for'],
      rows: [
        ['JSON', 'High', 'Medium', 'Public APIs, web/mobile apps'],
        ['XML', 'Medium', 'Low-medium', 'Legacy enterprise, SOAP, document validation'],
        ['Protobuf', 'Low', 'High', 'gRPC, internal microservices'],
        ['Avro', 'Low', 'High', 'Kafka/events/data pipelines'],
        ['MessagePack', 'Low', 'High', 'Compact JSON-like payloads'],
      ],
    },
    realWorldExamples: [
      { company: 'Public banking API', icon: '🏦', description: 'Uses JSON because fintech partners need easy debugging and broad language support.' },
      { company: 'Risk microservice', icon: '⚡', description: 'Uses Protobuf/gRPC to reduce latency and provide generated typed clients.' },
      { company: 'Kafka event pipeline', icon: '📡', description: 'Uses Avro with schema registry so consumers can evolve safely.' },
      { company: 'Legacy settlement rail', icon: '🏛️', description: 'Uses XML because existing enterprise systems and compliance tooling expect it.' },
    ],
    interviewQuestions: [
      {
        question: 'Why not use Protobuf for every API?',
        answer: 'Protobuf is efficient and typed, but public consumers lose easy browser debugging and may need generated clients. JSON is often better for public developer experience. Protobuf shines internally where teams control both sides and performance matters.',
      },
      {
        question: 'How do you evolve an API schema safely?',
        answer: 'Add optional fields, keep old fields until clients migrate, avoid changing field meaning, version breaking changes, publish changelogs, monitor usage, and use schema compatibility checks for event formats.',
      },
      {
        question: 'What problems can JSON cause in money APIs?',
        answer: 'Floating-point precision and ambiguous types. Money should be sent as decimal strings or integer minor units plus currency, not as unsafe floating-point numbers.',
      },
    ],
    commonMistakes: [
      'Sending money as floating-point JSON numbers.',
      'Removing or renaming fields without knowing which clients still depend on them.',
      'Choosing binary formats for public APIs without considering developer experience.',
      'Ignoring dates/time zones and letting every client invent its own format.',
      'Forgetting schema compatibility in event streams.',
    ],
  },
  {
    moduleId: 'api-architectural-styles',
    tagline: 'API style is the communication shape: resources, queries, remote procedures, streams, callbacks, or real-time channels.',
    introduction: {
      layman: 'Different API styles are like different vehicles. A bike, car, train, and airplane all move people, but each fits a different route.',
      analogy: 'REST is like a library catalog, GraphQL is like a custom order form, gRPC is like a private high-speed hotline, WebSocket is like an open phone call, and webhooks are like a doorbell callback.',
      whyMatters: 'In system design interviews, choosing an API style shows whether you understand client needs, latency, data shape, coupling, streaming, network constraints, and operational complexity.',
    },
    subTopics: [
      {
        title: 'REST',
        icon: '🌐',
        layman: 'REST organizes APIs around resources like accounts, cards, transactions, and users.',
        technical: 'REST uses HTTP methods, URLs, status codes, headers, caching semantics, and stateless requests. It is simple, widely supported, and excellent for CRUD-like resources. Pain points include over-fetching, under-fetching, and too many endpoints for complex screens.',
        example: 'GET /v1/accounts/123/transactions?limit=20 returns the latest transactions.',
        whenToUse: 'Use REST for public APIs, simple resources, broad compatibility, and developer-friendly contracts.',
      },
      {
        title: 'GraphQL',
        icon: '🕸️',
        layman: 'GraphQL lets the client ask for exactly the fields it needs.',
        technical: 'GraphQL exposes a typed schema and resolves fields from backend services. It reduces over-fetching for complex UIs but introduces resolver complexity, N+1 problems, caching challenges, authorization per field, and query cost controls.',
        example: 'A dashboard asks for account balance, last 5 transactions, card status, and loan offers in one query.',
      },
      {
        title: 'gRPC',
        icon: '🚀',
        layman: 'gRPC is a fast internal service-to-service protocol with generated clients.',
        technical: 'gRPC uses HTTP/2 and Protocol Buffers. It supports unary calls, server streaming, client streaming, and bidirectional streaming. It is efficient and strongly typed, but less browser-native and less human-debuggable than JSON/REST.',
        example: 'Transfer service calls RiskScoreService.CheckTransfer over gRPC before approving a high-value payment.',
      },
      {
        title: 'WebSocket and Webhooks',
        icon: '📡',
        layman: 'WebSocket keeps a live connection open. Webhooks notify another server when something happens.',
        technical: 'WebSocket is bidirectional and stateful, useful for chat, live prices, collaboration, and games. Webhooks are outbound HTTP callbacks, useful for partner integrations and async status updates. Both need retry/reconnect and security design.',
        example: 'A bank uses WebSocket for live trading prices and webhooks to notify a merchant when a payout settles.',
      },
    ],
    comparison: {
      caption: 'API styles at a glance',
      columns: ['Style', 'Best for', 'Strength', 'Trade-off'],
      rows: [
        ['REST', 'Resource APIs', 'Simple and universal', 'Can over/under-fetch'],
        ['GraphQL', 'Flexible UI reads', 'Client selects fields', 'Query complexity and caching'],
        ['gRPC', 'Internal services', 'Fast and typed', 'Less browser-friendly'],
        ['WebSocket', 'Live two-way updates', 'Low-latency push', 'Connection state and scaling'],
        ['Webhook', 'Server callbacks', 'Decoupled notifications', 'Retries and receiver reliability'],
        ['SOAP', 'Legacy enterprise', 'Strict contracts and WS-*', 'Verbose and heavyweight'],
      ],
    },
    realWorldExamples: [
      { company: 'GitHub', icon: '🐙', description: 'Offers REST and GraphQL APIs because different developer workflows need different query flexibility.' },
      { company: 'Bank microservices', icon: '🏦', description: 'Use REST for partners, gRPC internally for risk and ledger calls, and webhooks for settlement notifications.' },
      { company: 'Trading platform', icon: '📈', description: 'Uses WebSockets to push market ticks and order book updates in real time.' },
      { company: 'Enterprise SOAP systems', icon: '🏛️', description: 'Older insurance and banking integrations still use SOAP/XML for formal contracts and tooling.' },
    ],
    interviewQuestions: [
      {
        question: 'How do you choose between REST, GraphQL, and gRPC?',
        answer: 'REST for simple public resource APIs and broad compatibility. GraphQL for complex client-driven reads where one screen needs data from many sources. gRPC for internal low-latency typed service calls where both sides are controlled by your organization.',
      },
      {
        question: 'When would you use webhooks instead of polling?',
        answer: 'When the provider knows when an event occurs and can call the consumer. Webhooks reduce waste and latency compared to repeatedly polling for status, but need signature verification, retries, idempotency, and delivery logs.',
      },
      {
        question: 'Why is GraphQL not automatically better than REST?',
        answer: 'GraphQL moves complexity to query planning, resolvers, authorization, caching, cost limits, and operational safety. For simple CRUD, REST is easier and more cache-friendly.',
      },
    ],
    commonMistakes: [
      'Choosing GraphQL only because it is fashionable.',
      'Using WebSockets for simple one-way notifications where SSE or polling would be simpler.',
      'Exposing gRPC publicly without thinking about client support and debugging.',
      'Treating webhooks as fire-and-forget without retries, signatures, and idempotency.',
      'Mixing styles randomly without explaining why each style fits its workload.',
    ],
  },
  {
    moduleId: 'rest-api-design',
    tagline: 'REST API design is about clear resources, predictable behavior, and contracts developers can trust.',
    introduction: {
      layman: 'A good REST API feels like a well-organized filing cabinet: the names make sense, actions are predictable, and errors explain what went wrong.',
      analogy: 'Like a city map: streets have names, buildings have addresses, and signs tell you where to go. REST gives resources addresses and uses HTTP methods as actions.',
      whyMatters: 'REST is still the most common API style in interviews and real companies. Strong REST design shows that you understand resource modeling, HTTP semantics, pagination, versioning, errors, auth, and backward compatibility.',
    },
    subTopics: [
      {
        title: 'Resource Naming',
        icon: '🏷️',
        layman: 'Use nouns for things: accounts, transactions, cards, users.',
        technical: 'REST endpoints should model resources, not implementation steps. Prefer plural nouns and hierarchy where it clarifies ownership. Avoid verbs in paths when HTTP methods already express actions.',
        example: 'Good: GET /v1/accounts/123/transactions. Weak: GET /getTransactionsForAccount?id=123.',
      },
      {
        title: 'HTTP Methods and Status Codes',
        icon: '📮',
        layman: 'GET reads, POST creates, PUT replaces, PATCH partially updates, DELETE removes.',
        technical: 'Use status codes consistently: 200/201/202 for success, 204 for no body, 400 validation, 401 unauthenticated, 403 forbidden, 404 not found, 409 conflict, 422 semantic validation, 429 rate limited, 5xx server failure.',
        example: 'POST /v1/transfers returns 201 Created if completed immediately or 202 Accepted if processing asynchronously.',
      },
      {
        title: 'Pagination, Filtering, and Sorting',
        icon: '📄',
        layman: 'Do not return a million transactions in one response.',
        technical: 'Use cursor/keyset pagination for large or changing datasets. Offset pagination is simpler but can be slow and inconsistent at scale. Filtering and sorting should map to indexed fields and have clear limits.',
        example: 'GET /transactions?accountId=123&limit=50&cursor=eyJpZCI6... returns nextCursor for the next page.',
      },
      {
        title: 'Versioning and Errors',
        icon: '🧯',
        layman: 'Old apps should keep working when the API evolves.',
        technical: 'Version breaking changes with URL, header, or media type strategy. Error responses should have stable machine-readable codes, human-readable messages, request ids, and field-level validation details.',
        example: '{ "error": { "code": "INSUFFICIENT_FUNDS", "message": "Balance is too low", "requestId": "req_123" } }',
      },
    ],
    comparison: {
      caption: 'REST method semantics',
      columns: ['Method', 'Purpose', 'Idempotent?', 'Example'],
      rows: [
        ['GET', 'Read', 'Yes', 'GET /accounts/123'],
        ['POST', 'Create/action', 'No by default', 'POST /transfers'],
        ['PUT', 'Replace', 'Yes', 'PUT /profile/address'],
        ['PATCH', 'Partial update', 'Depends', 'PATCH /cards/7/limits'],
        ['DELETE', 'Remove', 'Yes', 'DELETE /beneficiaries/9'],
      ],
    },
    realWorldExamples: [
      { company: 'PayBank', icon: '🏦', description: 'Uses REST resources for accounts, cards, beneficiaries, transactions, and statements.' },
      { company: 'Stripe', icon: '💳', description: 'Provides predictable REST-like APIs with idempotency keys and consistent error objects.' },
      { company: 'GitHub', icon: '🐙', description: 'Uses REST endpoints for repositories, issues, pull requests, users, and teams.' },
      { company: 'E-commerce API', icon: '🛒', description: 'Models products, carts, orders, payments, refunds, and shipments as resources.' },
    ],
    interviewQuestions: [
      {
        question: 'How would you design a transfer API?',
        answer: 'POST /v1/transfers with source account, destination, amount, currency, idempotency key, and optional note. Validate auth and permissions, return transfer id/status, use 201 or 202, and expose GET /v1/transfers/{id} for status. Use consistent error codes like INSUFFICIENT_FUNDS and LIMIT_EXCEEDED.',
      },
      {
        question: 'Offset vs cursor pagination: which would you choose for transactions?',
        answer: 'Cursor pagination. Transactions are append-heavy and can change while the user pages. Cursor/keyset pagination is stable and efficient with indexes. Offset is okay for small admin lists but gets slow and can skip/duplicate rows under writes.',
      },
      {
        question: 'How do you version REST APIs?',
        answer: 'Use a clear strategy such as /v1 for public APIs, avoid breaking changes inside a version, add optional fields for compatible changes, publish deprecation windows, and monitor client usage before removing old versions.',
      },
    ],
    commonMistakes: [
      'Using verbs everywhere instead of resource names.',
      'Returning 200 OK for every success and every failure.',
      'Building pagination after the dataset is already huge.',
      'Changing response meanings without versioning or migration.',
      'Leaking internal exception messages to API consumers.',
    ],
  },
  {
    moduleId: 'graphql',
    tagline: 'GraphQL lets clients request exactly the shape of data they need from a typed schema.',
    introduction: {
      layman: 'Instead of the server deciding one fixed response for every screen, GraphQL lets the client ask for the exact fields it needs.',
      analogy: 'Like ordering a custom thali: you pick rice, dal, paneer, and salad instead of accepting a fixed plate with things you may not want.',
      whyMatters: 'GraphQL is common in complex frontends where screens need data from many services. It solves some REST pain but introduces new production concerns: resolver performance, N+1 queries, caching, auth, query limits, and schema governance.',
    },
    subTopics: [
      {
        title: 'Schema, Queries, and Mutations',
        icon: '📘',
        layman: 'The schema is the menu. Queries read data. Mutations change data.',
        technical: 'A GraphQL schema defines types, fields, arguments, queries, mutations, and sometimes subscriptions. Clients send a query document and receive a response with the same shape. Mutations should still use idempotency when they create side effects.',
        example: 'A mobile app queries account { balance transactions(limit: 5) { amount merchant } } in one request.',
      },
      {
        title: 'Resolvers',
        icon: '🔧',
        layman: 'Resolvers are functions that know how to fetch each field.',
        technical: 'Resolvers may call databases, REST services, gRPC services, caches, or other data loaders. The resolver tree executes based on requested fields, so expensive fields should be protected by auth and cost limits.',
        example: 'Account.balance resolves from ledger service, account.cards resolves from card service, and account.offers resolves from personalization service.',
      },
      {
        title: 'N+1 Problem',
        icon: '🧨',
        layman: 'A query that looks like one request can accidentally create hundreds of backend calls.',
        technical: 'Nested resolvers can fetch child data one parent at a time. DataLoader-style batching and caching group loads by key. Without it, a list of 100 accounts may trigger 100 separate transaction calls.',
        example: 'Querying 50 orders and each order\'s shipment separately can cause 51 database/API calls instead of 2 batched calls.',
      },
      {
        title: 'Security and Limits',
        icon: '🛡️',
        layman: 'If clients can ask for anything, you must control how much they can ask for.',
        technical: 'Use depth limits, query cost analysis, persisted queries, field-level authorization, rate limits, timeouts, introspection policy, and logging of slow/expensive queries.',
        example: 'A bank blocks a deeply nested query that asks for every account, every transaction, every merchant, and every refund in one request.',
      },
    ],
    comparison: {
      caption: 'REST vs GraphQL',
      columns: ['Dimension', 'REST', 'GraphQL'],
      rows: [
        ['Response shape', 'Server-defined endpoint', 'Client-selected fields'],
        ['Caching', 'HTTP-friendly', 'Needs persisted queries or custom caching'],
        ['Complex screens', 'Multiple requests or BFF', 'Often one query'],
        ['Operational risk', 'Endpoint explosion', 'Expensive arbitrary queries'],
        ['Best for', 'Simple resources/public APIs', 'Complex UI aggregation'],
      ],
    },
    realWorldExamples: [
      { company: 'Mobile banking dashboard', icon: '📱', description: 'Uses GraphQL to fetch accounts, cards, offers, and recent transactions in one screen-specific request.' },
      { company: 'GitHub GraphQL API', icon: '🐙', description: 'Lets clients fetch repository, issue, pull request, and user fields in one custom query.' },
      { company: 'E-commerce storefront', icon: '🛒', description: 'Product pages can request only the fields needed for desktop or mobile layouts.' },
      { company: 'Admin analytics', icon: '📊', description: 'Internal dashboards use GraphQL to compose data from many backend services.' },
    ],
    interviewQuestions: [
      {
        question: 'When would you choose GraphQL over REST?',
        answer: 'When clients need flexible, screen-specific data from multiple sources and REST would cause over-fetching, under-fetching, or many round trips. I would avoid GraphQL for simple CRUD or when HTTP caching/public developer simplicity matters more.',
      },
      {
        question: 'How do you prevent GraphQL from taking down your backend?',
        answer: 'Use query depth and cost limits, persisted queries, DataLoader batching, field-level auth, request timeouts, rate limits, caching, and metrics for slow resolvers and expensive query shapes.',
      },
      {
        question: 'What is the N+1 problem in GraphQL?',
        answer: 'A parent list resolver fetches N items, then each child resolver makes another call per item. A query for 100 orders can become 101 backend calls. Batch by ids and cache within the request using DataLoader-style loaders.',
      },
    ],
    commonMistakes: [
      'Assuming GraphQL automatically improves performance.',
      'Skipping query complexity limits.',
      'Doing authorization only at the top-level query and forgetting field-level permissions.',
      'Letting every frontend invent unreviewed production queries.',
      'Ignoring caching until traffic grows.',
    ],
  },
  {
    moduleId: 'grpc',
    tagline: 'gRPC is a high-performance RPC framework for typed service-to-service communication.',
    introduction: {
      layman: 'gRPC is like giving services a shared typed phonebook. One service can call another function directly, with generated code and compact messages.',
      analogy: 'Like a dedicated railway between two company offices: faster and more structured than public roads, but it needs both sides to agree on tracks and schedules.',
      whyMatters: 'Large systems often use REST externally and gRPC internally. Interviewers want to hear when strong typing, low latency, streaming, and generated clients are worth the added operational complexity.',
    },
    subTopics: [
      {
        title: 'Protocol Buffers',
        icon: '📦',
        layman: 'Protobuf is the schema language and compact message format gRPC usually uses.',
        technical: 'Services define .proto files with messages and RPC methods. Code generation creates clients and servers in many languages. Field numbers preserve wire compatibility, so evolution must be careful.',
        example: 'RiskService.CheckTransfer(CheckTransferRequest) returns riskScore, decision, and reason codes.',
      },
      {
        title: 'HTTP/2 and Streaming',
        icon: '🌊',
        layman: 'gRPC can send one request/response or streams of messages.',
        technical: 'gRPC uses HTTP/2 multiplexing, headers, status trailers, flow control, and streaming modes: unary, server streaming, client streaming, and bidirectional streaming.',
        example: 'A fraud engine streams live transaction events to a monitoring client for investigation.',
      },
      {
        title: 'Production Concerns',
        icon: '🛠️',
        layman: 'Fast calls still need timeouts, retries, auth, logging, and monitoring.',
        technical: 'Use deadlines, retries only for safe/idempotent calls, mTLS or service identity, load balancing, health checks, interceptors, structured errors, and observability. Debugging binary payloads requires tooling.',
        example: 'Transfer service sets a 150 ms deadline for RiskService so checkout does not hang forever.',
      },
      {
        title: 'gRPC vs REST',
        icon: '⚖️',
        layman: 'gRPC is great inside your company; REST is often easier for external developers.',
        technical: 'gRPC gives stronger contracts and better performance, while REST/JSON is more browser-friendly, cache-friendly, and human-debuggable. Public gRPC APIs need client generation and gateway support.',
        example: 'PayBank exposes REST to partners but uses gRPC between Transfer, Risk, Ledger, and Notification services.',
      },
    ],
    comparison: {
      caption: 'REST vs gRPC',
      columns: ['Dimension', 'REST/JSON', 'gRPC/Protobuf'],
      rows: [
        ['Developer debugging', 'Easy with curl/browser', 'Needs gRPC tooling'],
        ['Performance', 'Good', 'Usually better'],
        ['Typing', 'Convention/OpenAPI optional', 'Schema-first generated clients'],
        ['Browser support', 'Native', 'Needs gRPC-Web/proxy'],
        ['Best for', 'Public APIs', 'Internal microservices'],
      ],
    },
    realWorldExamples: [
      { company: 'Internal banking services', icon: '🏦', description: 'Transfer service calls risk, ledger, and limits services using typed gRPC contracts.' },
      { company: 'Low-latency trading', icon: '📈', description: 'Internal services use compact binary RPCs where milliseconds and typed contracts matter.' },
      { company: 'Kubernetes ecosystem', icon: '☸️', description: 'Many cloud-native control-plane components use gRPC-like typed APIs internally.' },
      { company: 'ML inference', icon: '🤖', description: 'Model-serving systems often use RPC contracts for predictable request and response schemas.' },
    ],
    interviewQuestions: [
      {
        question: 'When would you choose gRPC?',
        answer: 'For internal service-to-service communication where both sides are controlled, performance matters, strong typed contracts help, and streaming may be useful. I would be cautious for public browser-heavy APIs.',
      },
      {
        question: 'What production settings matter for gRPC?',
        answer: 'Deadlines, retries for safe calls, mTLS/service auth, load balancing, health checks, interceptors, structured errors, metrics, tracing, and compatibility rules for .proto evolution.',
      },
      {
        question: 'Can gRPC replace message queues?',
        answer: 'No. gRPC is request/response or streaming communication between live services. Queues provide durable buffering, decoupling, retries, and async processing when consumers may be down or slower than producers.',
      },
    ],
    commonMistakes: [
      'Using gRPC externally without considering client tooling and browser support.',
      'Skipping deadlines, causing cascading latency.',
      'Retrying non-idempotent RPCs blindly.',
      'Changing Protobuf field numbers or meanings incorrectly.',
      'Assuming faster serialization solves bad service boundaries.',
    ],
  },
  {
    moduleId: 'api-gateways',
    tagline: 'An API gateway is the controlled front door in front of backend services.',
    introduction: {
      layman: 'Instead of every client knowing every service, clients call one gateway. The gateway checks rules and sends the request to the right backend.',
      analogy: 'Like a hotel concierge: guests ask the concierge, the concierge checks who they are, routes the request to housekeeping or billing, and keeps a record.',
      whyMatters: 'Microservices create many APIs. A gateway centralizes routing, authentication hooks, rate limiting, logging, transformations, canary rollout, and version migration. It is often the first box you draw after client and load balancer.',
    },
    subTopics: [
      {
        title: 'Routing',
        icon: '🧭',
        layman: 'The gateway knows which service should handle which request.',
        technical: 'Route by path, host, method, header, version, tenant, region, or feature flag. Gateways can also split traffic for canary releases and route legacy clients to older versions.',
        example: '/v1/transfers routes to Transfer Service, /v1/cards routes to Card Service, and /partner/* routes through partner-specific policies.',
      },
      {
        title: 'Cross-Cutting Concerns',
        icon: '🧰',
        layman: 'The gateway handles repeated chores so every service does not rebuild them differently.',
        technical: 'Common gateway responsibilities include TLS termination, auth token verification, rate limits, request validation, CORS, logging, tracing, response compression, WAF integration, and API key enforcement.',
        example: 'PayBank gateway validates JWT signature, adds x-request-id, applies partner quota, and forwards trusted identity headers to internal services.',
      },
      {
        title: 'Aggregation and BFF',
        icon: '🧩',
        layman: 'Sometimes one frontend needs one tailored API instead of calling five services.',
        technical: 'A gateway or backend-for-frontend can aggregate calls for mobile/web screens. This reduces frontend round trips but can make the gateway too business-heavy if abused.',
        example: 'Mobile home endpoint combines accounts, cards, offers, and unread alerts into one response.',
      },
      {
        title: 'Failure and Bottleneck Risks',
        icon: '⚠️',
        layman: 'If the front door breaks, everyone is stuck outside.',
        technical: 'Gateways must be horizontally scaled, highly available, monitored, and kept thin enough to avoid becoming a monolith. Use timeouts, circuit breakers, graceful degradation, and regional redundancy.',
        example: 'If Partner Gateway latency spikes, PayBank can shed partner traffic while keeping retail mobile APIs healthy.',
      },
    ],
    comparison: {
      caption: 'Gateway responsibilities',
      columns: ['Concern', 'Gateway handles?', 'Service still handles?'],
      rows: [
        ['TLS termination', 'Yes', 'Usually no'],
        ['Token verification', 'Often', 'Yes for defense in depth'],
        ['Business authorization', 'Sometimes coarse', 'Yes, source of truth'],
        ['Rate limiting', 'Yes', 'Sometimes endpoint-specific too'],
        ['Business logic', 'Avoid heavy logic', 'Yes'],
      ],
    },
    realWorldExamples: [
      { company: 'PayBank API edge', icon: '🏦', description: 'Routes mobile, partner, and admin API traffic to different backend services with separate policies.' },
      { company: 'Kong / Apigee / AWS API Gateway', icon: '🚪', description: 'Managed or dedicated gateway products provide routing, keys, quotas, auth plugins, and analytics.' },
      { company: 'Netflix-style edge', icon: '🎬', description: 'Edge services aggregate backend calls for device-specific experiences.' },
      { company: 'Enterprise partner platform', icon: '🤝', description: 'Gateways enforce partner keys, quotas, audit logs, and version-specific routing.' },
    ],
    interviewQuestions: [
      {
        question: 'Why use an API gateway in microservices?',
        answer: 'To give clients one stable entry point and centralize routing, auth hooks, rate limits, logging, tracing, request validation, version routing, and traffic shaping. It reduces duplicated edge logic across services.',
      },
      {
        question: 'What should not live in the gateway?',
        answer: 'Deep business logic and authoritative permissions that belong to domain services. The gateway can do coarse checks, but services should still enforce critical rules.',
      },
      {
        question: 'How do you avoid the gateway becoming a single point of failure?',
        answer: 'Run multiple gateway instances across zones/regions behind load balancers, use health checks, autoscaling, config validation, canary rollout, observability, timeouts, and rollback strategy.',
      },
    ],
    commonMistakes: [
      'Putting too much business logic in the gateway.',
      'Trusting gateway auth so much that backend services skip authorization.',
      'Letting gateway config changes deploy without tests.',
      'Not setting timeouts and circuit breakers for backend calls.',
      'Using one shared quota for all clients instead of per-tenant/client limits.',
    ],
  },
  {
    moduleId: 'rate-limiting',
    tagline: 'Rate limiting protects APIs by controlling how much traffic each caller can send.',
    introduction: {
      layman: 'Rate limiting is a bouncer for your API. It lets normal users in and slows down abusive or broken clients.',
      analogy: 'Like a toll booth controlling how many cars enter a bridge per minute so the bridge does not jam.',
      whyMatters: 'APIs are shared infrastructure. A buggy app, bot, partner integration, or attack can overload services. Rate limiting keeps systems fair, reliable, and cost-controlled.',
    },
    subTopics: [
      {
        title: 'What to Limit',
        icon: '🎯',
        layman: 'Different callers and endpoints need different limits.',
        technical: 'Limit by user id, API key, IP, tenant, device, endpoint, region, or risk level. Money-moving APIs often have stricter limits than read-only APIs. Limits should return 429 with retry-after metadata.',
        example: 'A partner can call GET /transactions 600 times/minute but POST /transfers only 60 times/minute.',
      },
      {
        title: 'Token Bucket',
        icon: '🪣',
        layman: 'Tokens refill over time. Each request spends a token. Bursts are allowed until the bucket empties.',
        technical: 'Token bucket supports controlled bursts while enforcing an average rate. It is common for API gateways because users may naturally send brief bursts.',
        example: 'Mobile app gets 100 tokens and refills at 10/sec. Opening a dashboard can burst several calls without being blocked.',
      },
      {
        title: 'Fixed, Sliding, and Leaky Windows',
        icon: '⏱️',
        layman: 'Different algorithms count traffic differently.',
        technical: 'Fixed window is simple but has boundary burst problems. Sliding log is accurate but memory-heavy. Sliding counter approximates well. Leaky bucket smooths traffic at a steady output rate.',
        example: 'An API gateway uses sliding window counters in Redis to track partner traffic over the last minute.',
      },
      {
        title: 'Distributed Rate Limiting',
        icon: '🌍',
        layman: 'With many API servers, they must agree on the count.',
        technical: 'Use centralized stores like Redis, local warm caches with periodic sync, or gateway-native distributed counters. The trade-off is accuracy vs latency vs availability. Fail-open and fail-closed policies must be explicit.',
        example: 'If Redis rate-limit store is down, PayBank fails closed for transfer APIs but fails open with lower limits for read-only status APIs.',
      },
    ],
    comparison: {
      caption: 'Rate limiting algorithms',
      columns: ['Algorithm', 'Strength', 'Weakness', 'Best for'],
      rows: [
        ['Fixed window', 'Simple', 'Boundary bursts', 'Basic quotas'],
        ['Sliding log', 'Accurate', 'Memory-heavy', 'Strict audit needs'],
        ['Sliding counter', 'Good balance', 'Approximate', 'API gateways'],
        ['Token bucket', 'Allows bursts', 'Needs token state', 'Public APIs/mobile apps'],
        ['Leaky bucket', 'Smooth output', 'Can add delay/drop', 'Traffic shaping'],
      ],
    },
    realWorldExamples: [
      { company: 'PayBank partners', icon: '🏦', description: 'Partners receive quotas by API key, endpoint, and contract tier.' },
      { company: 'GitHub API', icon: '🐙', description: 'Uses rate limits so one script cannot consume shared API capacity for everyone.' },
      { company: 'Login APIs', icon: '🔐', description: 'Rate limits prevent brute-force password and OTP attempts.' },
      { company: 'AI APIs', icon: '🤖', description: 'Limit requests and tokens to control cost and protect GPU capacity.' },
    ],
    interviewQuestions: [
      {
        question: 'How would you design rate limiting for a public API?',
        answer: 'Identify keys such as API key/user/IP, define per-endpoint quotas, use token bucket or sliding window in a low-latency shared store, return 429 with retry-after, add dashboards, allow tiered limits, and have explicit fail-open/fail-closed behavior.',
      },
      {
        question: 'Why not rate-limit only by IP?',
        answer: 'Many users can share an IP behind NAT, and attackers can rotate IPs. Use API keys, user ids, tenant ids, device fingerprints, and endpoint-specific policies where possible.',
      },
      {
        question: 'What is the difference between rate limiting and throttling?',
        answer: 'They are often used interchangeably, but rate limiting usually rejects or controls requests above a quota, while throttling can slow or shape traffic to a target rate.',
      },
    ],
    commonMistakes: [
      'Using only IP-based limits for authenticated APIs.',
      'Returning generic 500 errors instead of 429 with retry guidance.',
      'Applying the same limit to read-only and money-moving endpoints.',
      'Forgetting rate limits for login, OTP, password reset, and webhook endpoints.',
      'Building distributed counters without thinking about latency and failure mode.',
    ],
  },
  {
    moduleId: 'authentication-authorization',
    tagline: 'Authentication proves who you are; authorization decides what you can do.',
    introduction: {
      layman: 'Logging in proves your identity. Permission checks decide whether you can view an account, approve a payment, or manage users.',
      analogy: 'Authentication is showing your ID at reception. Authorization is whether your badge opens the trading floor, server room, or only the lobby.',
      whyMatters: 'Security questions appear in almost every real design. Mixing up authn and authz causes serious data leaks: a valid user might access another user\'s account if authorization is weak.',
    },
    subTopics: [
      {
        title: 'Authentication',
        icon: '🪪',
        layman: 'The system verifies the caller\'s identity.',
        technical: 'Authentication methods include password, OTP, biometrics, passkeys, certificates, API keys, SSO/OIDC, and mTLS. Strong systems add MFA, risk-based checks, lockouts, and secure credential storage.',
        example: 'PayBank verifies password plus device-bound OTP before issuing tokens.',
      },
      {
        title: 'Authorization',
        icon: '🚪',
        layman: 'The system checks whether the caller can perform this action on this resource.',
        technical: 'Authorization models include RBAC, ABAC, ACLs, scopes, ownership checks, and policy engines. It must happen server-side and often inside domain services, not only at the gateway.',
        example: 'A corporate accountant can create payroll drafts but only a finance approver can release payments above ₹10 lakh.',
      },
      {
        title: 'Object-Level Authorization',
        icon: '🎯',
        layman: 'Being logged in does not mean you can access every object.',
        technical: 'Broken object-level authorization is a common API vulnerability. Every request for /accounts/{id}, /orders/{id}, or /documents/{id} must verify that the caller owns or is permitted for that specific object.',
        example: 'GET /accounts/999 must check that account 999 belongs to the logged-in customer or their authorized corporate role.',
      },
      {
        title: 'Defense in Depth',
        icon: '🛡️',
        layman: 'Do not rely on only one layer to protect everything.',
        technical: 'Gateways can verify tokens and coarse scopes, but backend services should enforce business permissions. Logs, audits, alerts, least privilege, and periodic access reviews reduce blast radius.',
        example: 'Gateway checks token scope transfer:create; Transfer Service checks account ownership, daily limit, approver role, and risk status.',
      },
    ],
    comparison: {
      caption: 'Authentication vs authorization',
      columns: ['Concept', 'Question answered', 'Examples', 'Common failure'],
      rows: [
        ['Authentication', 'Who are you?', 'Password, OTP, SSO, certificate', 'Weak login or stolen credential'],
        ['Authorization', 'What can you do?', 'RBAC, scopes, ownership checks', 'Logged-in user accesses another user\'s data'],
      ],
    },
    realWorldExamples: [
      { company: 'PayBank customer app', icon: '🏦', description: 'Authenticates customers with MFA and authorizes account access by ownership.' },
      { company: 'Corporate banking', icon: '🏢', description: 'Uses roles and approval limits so makers and checkers have different payment powers.' },
      { company: 'Cloud console', icon: '☁️', description: 'Authenticates employees but authorizes actions through IAM policies.' },
      { company: 'Healthcare portal', icon: '🏥', description: 'Doctors, patients, and billing staff authenticate similarly but receive different record permissions.' },
    ],
    interviewQuestions: [
      {
        question: 'Where should authorization checks happen?',
        answer: 'At multiple layers. The gateway can verify token and broad scopes, but domain services must enforce object ownership and business permissions because they understand the data and rules.',
      },
      {
        question: 'What is broken object-level authorization?',
        answer: 'A user can change an id in the URL and access another user\'s resource because the API checks login but not ownership/permission for that object. It is one of the most serious API security bugs.',
      },
      {
        question: 'How do RBAC and ABAC differ?',
        answer: 'RBAC grants permissions by role, such as admin or approver. ABAC uses attributes such as department, region, amount, device risk, time, and resource owner. Enterprises often combine both.',
      },
    ],
    commonMistakes: [
      'Checking authentication but skipping object-level authorization.',
      'Trusting client-side hidden buttons as permission enforcement.',
      'Putting all authorization only in the API gateway.',
      'Giving broad admin roles instead of least privilege.',
      'Not logging sensitive authorization decisions for audit.',
    ],
  },
  {
    moduleId: 'session-vs-token-auth',
    tagline: 'Sessions store login state on the server; tokens carry verifiable login state with the client.',
    introduction: {
      layman: 'A session is like a coat-check ticket: the server remembers you. A token is like a signed wristband: services can inspect it without asking the login server every time.',
      analogy: 'At an office, a receptionist logbook is session-based. A signed badge that every door scanner can verify is token-based.',
      whyMatters: 'Choosing session vs token affects scalability, revocation, mobile support, browser security, microservices, and user experience. Real systems often use both in different places.',
    },
    subTopics: [
      {
        title: 'Session Authentication',
        icon: '🎫',
        layman: 'The server stores your session and the browser stores a session id cookie.',
        technical: 'After login, the server creates session state in memory/Redis/database and sends a cookie containing a random session id. Each request looks up the session. Sessions are easy to revoke but require shared state across servers.',
        example: 'PayBank admin portal stores employee session in Redis and can revoke it instantly after suspicious activity.',
        whenToUse: 'Use sessions for web apps needing central revocation, server-rendered apps, admin portals, and high-control enterprise flows.',
      },
      {
        title: 'Token Authentication',
        icon: '🔑',
        layman: 'The client carries a token and sends it with each API call.',
        technical: 'Tokens may be opaque or self-contained. Opaque tokens require introspection; JWTs can be verified locally. Tokens scale across services but revocation and leakage handling need careful expiration, rotation, and storage.',
        example: 'Mobile app sends Authorization: Bearer <access_token> to PayBank APIs.',
        whenToUse: 'Use tokens for mobile apps, APIs, microservices, partner integrations, and stateless service verification.',
      },
      {
        title: 'Cookies vs Authorization Header',
        icon: '🍪',
        layman: 'Browsers automatically send cookies. Apps usually attach bearer tokens explicitly.',
        technical: 'Cookies need HttpOnly, Secure, SameSite, CSRF protection, and domain/path control. Bearer tokens need secure storage and XSS protection. Neither is magically safe; threat model matters.',
        example: 'Admin web uses HttpOnly SameSite cookies. Mobile uses access token in secure OS storage.',
      },
      {
        title: 'Hybrid Designs',
        icon: '🔀',
        layman: 'Big companies often use sessions and tokens together.',
        technical: 'A web login may create a server-side session and issue short-lived access tokens for API calls. Refresh tokens or session state can renew access tokens. High-risk actions can require step-up authentication.',
        example: 'PayBank uses a web session for employee portal, short-lived JWT for API calls, and re-prompts MFA for approving large transfers.',
      },
    ],
    comparison: {
      caption: 'Session vs token trade-offs',
      columns: ['Dimension', 'Session', 'Token'],
      rows: [
        ['State', 'Server stores state', 'Client carries credential'],
        ['Revocation', 'Easy central revocation', 'Harder unless short-lived/denylist/introspection'],
        ['Scaling', 'Needs shared session store', 'Stateless verification possible'],
        ['Browser fit', 'Natural with cookies', 'Works but storage risks matter'],
        ['Mobile/API fit', 'Less common', 'Very common'],
      ],
    },
    realWorldExamples: [
      { company: 'Admin dashboard', icon: '🏢', description: 'Often uses server-side sessions for central control and immediate logout.' },
      { company: 'Mobile banking API', icon: '📱', description: 'Uses access and refresh tokens because mobile apps call stateless APIs.' },
      { company: 'Enterprise SSO', icon: '🗝️', description: 'SSO login may create both an identity provider session and application tokens.' },
      { company: 'Microservices', icon: '🧩', description: 'Services verify signed tokens or introspect opaque tokens before processing requests.' },
    ],
    interviewQuestions: [
      {
        question: 'Which is more secure: sessions or tokens?',
        answer: 'Neither universally. Sessions are easier to revoke centrally but need secure cookies and session storage. Tokens scale well but need short lifetimes, safe storage, rotation, and revocation strategy. Security depends on implementation and threat model.',
      },
      {
        question: 'Why are refresh tokens used?',
        answer: 'Access tokens should be short-lived to reduce damage if stolen. Refresh tokens allow the client to obtain new access tokens without asking the user to log in repeatedly, but they must be stored and rotated carefully.',
      },
      {
        question: 'How do you handle logout with JWTs?',
        answer: 'Use short access-token expiry, revoke/rotate refresh tokens, optionally maintain a denylist for high-risk tokens, and invalidate server-side sessions if the JWT was issued from one.',
      },
    ],
    commonMistakes: [
      'Storing long-lived tokens insecurely in browser localStorage without considering XSS.',
      'Using JWTs with very long expiration and no revocation plan.',
      'Forgetting CSRF protection for cookie-based session flows.',
      'Assuming stateless tokens mean no security state is needed anywhere.',
      'Not rotating refresh tokens.',
    ],
  },
  {
    moduleId: 'jwt',
    tagline: 'A JWT is a signed token format that can carry claims about a user or client.',
    introduction: {
      layman: 'A JWT is a tamper-evident note. Services can read it and verify that a trusted issuer signed it.',
      analogy: 'Like a sealed company letter saying “Rohit is employee 123 and can access finance reports.” Anyone can read it, but only the company can create a valid signature.',
      whyMatters: 'JWTs are widely used in APIs and SSO/OIDC. They are useful but easy to misuse: leaked tokens are bearer credentials, payloads are not secret, and revocation is not automatic.',
    },
    subTopics: [
      {
        title: 'JWT Anatomy',
        icon: '🧬',
        layman: 'A JWT has three parts: header, payload, and signature.',
        technical: 'Header declares algorithm and token type. Payload contains claims such as sub, iss, aud, exp, iat, scopes, roles, or tenant. Signature proves the token was issued by a trusted key and was not modified.',
        example: 'header.payload.signature, where payload might include sub=user_123 and scope=accounts:read.',
      },
      {
        title: 'Verification Flow',
        icon: '✅',
        layman: 'The API checks the signature and claims before trusting the token.',
        technical: 'Verify signature, issuer, audience, expiration, not-before, algorithm, key id, and required scopes. Fetch public keys from JWKS for asymmetric signing. Never accept unsigned tokens.',
        example: 'API gateway verifies PayBank issuer, audience=mobile-api, exp not expired, and scope=transfer:create.',
      },
      {
        title: 'Claims and Scopes',
        icon: '🏷️',
        layman: 'Claims describe who the token is for and what it can do.',
        technical: 'Keep claims minimal. Include stable identifiers and coarse scopes. Do not put secrets, sensitive PII, or rapidly changing permissions in long-lived JWTs. Services may still need database checks for object-level authorization.',
        example: 'JWT says customer can read accounts, but Account Service still checks whether account 999 belongs to that customer.',
      },
      {
        title: 'JWT Security',
        icon: '🛡️',
        layman: 'JWTs are powerful, but stolen tokens can be used until they expire.',
        technical: 'Use short expirations, refresh token rotation, secure storage, TLS, algorithm allowlists, key rotation, audience checks, and revocation for high-risk events. JWE encryption is separate from signed JWTs.',
        example: 'PayBank access JWT expires in 10 minutes; refresh token rotates and is revoked on device compromise.',
      },
    ],
    comparison: {
      caption: 'JWT terms',
      columns: ['Term', 'Meaning', 'Why it matters'],
      rows: [
        ['iss', 'Issuer', 'Who created the token'],
        ['sub', 'Subject', 'User/client identity'],
        ['aud', 'Audience', 'Which API should accept it'],
        ['exp', 'Expiration', 'Limits stolen token lifetime'],
        ['scope/roles', 'Permissions hints', 'Input to authorization decisions'],
      ],
    },
    realWorldExamples: [
      { company: 'OIDC login', icon: '🗝️', description: 'Identity providers issue ID tokens as JWTs so apps can verify logged-in user identity.' },
      { company: 'API gateway', icon: '🚪', description: 'Gateways verify JWT signatures before routing requests to backend services.' },
      { company: 'Mobile banking', icon: '📱', description: 'Short-lived access JWTs let mobile APIs authenticate calls without a session lookup on every request.' },
      { company: 'Service mesh', icon: '🧩', description: 'Internal services can pass signed identity tokens between services with strict audience checks.' },
    ],
    interviewQuestions: [
      {
        question: 'Is JWT payload encrypted?',
        answer: 'Usually no. A signed JWT is base64url-encoded and readable by anyone who has it. The signature prevents tampering, not reading. Do not put secrets in a normal JWT. Encryption uses JWE, which is separate.',
      },
      {
        question: 'What should an API verify before accepting a JWT?',
        answer: 'Signature, allowed algorithm, issuer, audience, expiration, not-before, key id, token type if relevant, and required scopes/claims. Then perform domain authorization separately.',
      },
      {
        question: 'Why are long-lived JWTs risky?',
        answer: 'Because they remain usable until expiration if stolen, and revocation is hard without server-side state. Use short-lived access tokens and refresh-token rotation.',
      },
    ],
    commonMistakes: [
      'Putting passwords, secrets, or sensitive PII in JWT payloads.',
      'Accepting tokens without checking audience.',
      'Allowing insecure or unexpected signing algorithms.',
      'Using JWT scopes as a replacement for object-level authorization.',
      'Creating long-lived JWTs with no revocation path.',
    ],
  },
  {
    moduleId: 'oauth-oauth2',
    tagline: 'OAuth 2.0 lets users delegate limited access to an app without sharing their password.',
    introduction: {
      layman: 'OAuth lets you allow one app to access something from another app without giving it your password.',
      analogy: 'Like giving a valet key to park your car. It can do one limited job; it is not your master key.',
      whyMatters: 'OAuth is everywhere: “Login with Google,” fintech account access, SaaS integrations, mobile authorization, and partner APIs. Interviews often test whether you know OAuth is authorization, while OpenID Connect adds authentication.',
    },
    subTopics: [
      {
        title: 'OAuth Roles',
        icon: '🎭',
        layman: 'OAuth has a user, an app, an authorization server, and an API server.',
        technical: 'Resource owner grants access. Client app requests access. Authorization server issues tokens. Resource server accepts access tokens and protects resources.',
        example: 'A budgeting app asks PayBank user consent to read transactions. PayBank authorization server issues a limited access token.',
      },
      {
        title: 'Authorization Code + PKCE',
        icon: '🔐',
        layman: 'The app redirects you to the trusted login page, then receives a code it can exchange for tokens.',
        technical: 'Authorization Code Flow with PKCE is the recommended flow for public clients like mobile/SPAs. PKCE protects against intercepted authorization codes by proving the app that started the flow is the app redeeming the code.',
        example: 'PayBank mobile app opens PayBank login, receives an auth code, sends code_verifier, and gets tokens.',
      },
      {
        title: 'Access and Refresh Tokens',
        icon: '🔁',
        layman: 'Access tokens are short-lived keys. Refresh tokens renew them.',
        technical: 'Access tokens authorize API calls. Refresh tokens obtain new access tokens and must be stored carefully. Rotation and reuse detection reduce token theft risk.',
        example: 'A partner gets transactions:read access for 90 days, but each access token lasts 10 minutes.',
      },
      {
        title: 'OpenID Connect',
        icon: '🪪',
        layman: 'OIDC adds login identity on top of OAuth.',
        technical: 'OAuth answers delegated access. OIDC adds ID tokens, userinfo, standard identity claims, discovery metadata, and login semantics. “Sign in with Google” is typically OIDC.',
        example: 'Employee SSO uses OIDC ID token to prove identity and OAuth access token to call APIs.',
      },
    ],
    comparison: {
      caption: 'OAuth vs OIDC',
      columns: ['Protocol', 'Main purpose', 'Token', 'Example'],
      rows: [
        ['OAuth 2.0', 'Delegated authorization', 'Access token', 'Allow app to read transactions'],
        ['OpenID Connect', 'Authentication on top of OAuth', 'ID token', 'Sign in with corporate identity'],
      ],
    },
    realWorldExamples: [
      { company: 'Google login', icon: '🔎', description: 'Apps use OAuth/OIDC flows so users can sign in without sharing their Google password.' },
      { company: 'Open banking', icon: '🏦', description: 'Users grant fintech apps scoped access to bank account data for a limited time.' },
      { company: 'Slack integrations', icon: '💬', description: 'Workspace admins grant apps limited scopes like channels:read or chat:write.' },
      { company: 'Enterprise SaaS', icon: '🏢', description: 'Admins authorize integrations between CRM, support tools, and analytics platforms.' },
    ],
    interviewQuestions: [
      {
        question: 'Is OAuth authentication or authorization?',
        answer: 'OAuth 2.0 is authorization: delegated access to resources. OpenID Connect builds authentication on top of OAuth by adding ID tokens and identity claims.',
      },
      {
        question: 'Why use PKCE?',
        answer: 'PKCE protects public clients from authorization-code interception. The client creates a code verifier/challenge; the token endpoint requires the verifier before exchanging the code.',
      },
      {
        question: 'What scopes would you use for a banking partner API?',
        answer: 'Use least privilege scopes like accounts:read, transactions:read, transfers:create, beneficiaries:read. Separate read and write scopes, require explicit user/admin consent, and set expiry.',
      },
    ],
    commonMistakes: [
      'Saying OAuth is login without mentioning OIDC.',
      'Using implicit flow for modern SPAs instead of Authorization Code with PKCE.',
      'Requesting broad scopes instead of least privilege.',
      'Treating refresh tokens casually or storing them insecurely.',
      'Skipping state parameter and CSRF protection in OAuth redirects.',
    ],
  },
  {
    moduleId: 'sso',
    tagline: 'Single Sign-On lets users log in once through a trusted identity provider and access many applications.',
    introduction: {
      layman: 'SSO means one corporate login gives you access to many approved apps without typing your password into each one.',
      analogy: 'Like checking in at the front desk once and receiving a badge that opens every room you are allowed to enter.',
      whyMatters: 'Enterprise systems rely on SSO for security, user lifecycle management, MFA, access reviews, compliance, and employee productivity. Interviewers expect SAML/OIDC basics and session/token trade-offs.',
    },
    subTopics: [
      {
        title: 'Identity Provider and Service Provider',
        icon: '🏢',
        layman: 'The identity provider logs you in. The app trusts that provider.',
        technical: 'The identity provider (IdP) authenticates the user and issues assertions or tokens. The service provider (SP) or application validates them and creates an app session. Trust is configured through metadata, certificates, client ids, redirect URIs, and policies.',
        example: 'Okta authenticates a PayBank employee; PayBank admin portal trusts Okta and creates an app session.',
      },
      {
        title: 'SAML vs OIDC',
        icon: '🔀',
        layman: 'SAML is older and XML-heavy. OIDC is newer and OAuth/JWT-based.',
        technical: 'SAML commonly uses XML assertions and browser redirects, widely used in older enterprise SaaS. OIDC uses OAuth 2.0 flows and ID tokens, common in cloud-native and mobile-friendly systems.',
        example: 'A legacy payroll tool uses SAML SSO; a new internal developer portal uses OIDC.',
      },
      {
        title: 'Session Management',
        icon: '🎫',
        layman: 'Logging out everywhere is harder than it sounds.',
        technical: 'There may be an IdP session and separate app sessions. Single logout, idle timeout, absolute timeout, step-up MFA, and device trust policies must be defined. Apps should react to user deprovisioning quickly.',
        example: 'When an employee leaves PayBank, disabling the IdP account must remove access to HR, finance, cloud console, and admin tools.',
      },
      {
        title: 'Enterprise Security Controls',
        icon: '🛡️',
        layman: 'SSO makes security controls centralized.',
        technical: 'SSO enables MFA, conditional access, device posture checks, IP/location rules, role/group claims, audit logs, and access reviews. Apps still need local authorization for domain-specific permissions.',
        example: 'Finance approvals require SSO login plus MFA and membership in finance-approvers group.',
      },
    ],
    comparison: {
      caption: 'SAML vs OIDC',
      columns: ['Dimension', 'SAML', 'OIDC'],
      rows: [
        ['Payload', 'XML assertion', 'JWT ID token'],
        ['Common age/use', 'Older enterprise SaaS', 'Modern web/mobile/cloud apps'],
        ['Built on', 'SAML protocol', 'OAuth 2.0'],
        ['Developer experience', 'Heavier', 'Generally simpler for modern apps'],
      ],
    },
    realWorldExamples: [
      { company: 'Corporate Google Workspace', icon: '🏢', description: 'Employees use one identity provider account to access email, docs, analytics, and internal tools.' },
      { company: 'PayBank employee portal', icon: '🏦', description: 'SSO plus MFA protects admin actions and centralizes employee deprovisioning.' },
      { company: 'SaaS admin console', icon: '⚙️', description: 'Enterprise customers connect their IdP so employees use company login rather than separate passwords.' },
      { company: 'Cloud provider console', icon: '☁️', description: 'SSO maps corporate groups to cloud roles and audit trails.' },
    ],
    interviewQuestions: [
      {
        question: 'How does SSO work at a high level?',
        answer: 'The user accesses an app, the app redirects to the identity provider, the IdP authenticates the user, returns a signed assertion/token, the app validates it, then creates an application session with appropriate roles/claims.',
      },
      {
        question: 'What is the difference between SSO and OAuth?',
        answer: 'SSO is the user experience/pattern of one login across applications. OAuth is a delegated authorization protocol. OIDC, built on OAuth, is commonly used to implement SSO authentication.',
      },
      {
        question: 'What happens when an employee leaves the company?',
        answer: 'Disable them in the IdP, revoke refresh tokens/sessions, propagate deprovisioning to apps, remove group memberships, and ensure critical apps perform timely session validation or short session lifetimes.',
      },
    ],
    commonMistakes: [
      'Thinking SSO means every app has identical permissions.',
      'Skipping local app authorization because the IdP authenticated the user.',
      'Ignoring logout and deprovisioning behavior.',
      'Not validating SAML signatures or OIDC issuer/audience.',
      'Letting group/role claims grow messy without access reviews.',
    ],
  },
];
